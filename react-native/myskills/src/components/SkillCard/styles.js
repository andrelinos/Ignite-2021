import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonSkill: {
        backgroundColor: '#1f1e25',
        padding: 16,
        borderRadius: 8,
        marginVertical: 8,
    },

    textSkill: {
        color: '#fff',
        fontSize: 22,
        fontWeight: 'bold',
    },
});
